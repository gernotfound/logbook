name: M8 Final Consumer Migration Temporary

on:
  push:
    branches:
      - feat/m8-domain-operations-v4
    paths:
      - .github/workflows/m8-final-consumer-migration.yml

permissions:
  contents: write

jobs:
  migrate:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
        with:
          ref: feat/m8-domain-operations-v4
          fetch-depth: 0
      - name: Apply migration
        run: node scripts/m8-final-consumer-migration.mjs
      - name: Commit migrated consumers and remove temporary machinery
        shell: bash
        run: |
          set -euo pipefail
          git rm -f scripts/m8-final-consumer-migration.mjs .github/workflows/m8-final-consumer-migration.yml
          git add -A
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git commit -m "M8: migrate remaining consumers to domain operations"
          git push origin HEAD:feat/m8-domain-operations-v4
